# MoE-Computer-V
Mixture of Experts for some Computer Vision tasks
## Environment 
### Docker
Build the image with `./docker/build.sh`  
Run the development env with `./docker.run.sh`  

### Conda
Create the environment with
```bash
conda env create -f environment.yml
```   

Activate the env with 
```bash
conda activate moe_computer
```